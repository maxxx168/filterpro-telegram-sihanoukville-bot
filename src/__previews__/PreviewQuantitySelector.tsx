import QuantitySelector from '@/components/QuantitySelector';

export default function PreviewQuantitySelector() {
  return <QuantitySelector language="en" onSelectQuantity={() => {}} onBack={() => {}} />;
}
